# Algorithms

Low-level signal algorithms used by metric pipelines.

## Files

- `algorithms/bp_delineator.py`: Blood-pressure beat delineation helpers.
- `algorithms/quality_detection.py`: Signal-quality feature/flag helpers.

Most users should call metric-level functions in `metrics/` instead of importing these directly.
