# Scripts

Operational scripts that are not part of the Streamlit runtime.

## Folders

- `scripts/diagnostics/`: troubleshooting helpers (for example PMU matching diagnostics).
- `scripts/pmu/`: standalone PMU auditing, visualization, and extraction scripts.

Run scripts from the repository root so relative defaults and imports resolve correctly.
